[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback : la page est mono-route, on renvoie tout vers index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
